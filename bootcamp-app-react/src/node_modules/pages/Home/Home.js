import React from 'react'
import Input from 'components/Input/Input'
import Button from 'components/Button/Button'
import './Home.css'

const Home = props => {
    return (
        <div className={'home_container'}>
          <form className="home_window">

              <p className={'home_window_title'}>Find a Code Bootcamp</p>
              <p className={'home_window_description'}>Find, rate and read reviews on coding bootcamps</p>
              <div className="home_window_inputs">
                  <Input
                      className={'home'}
                      title={'Miles From'}
                      name={'miles_from'}
                      id={'first_input'}
                    />
                  <Input
                      className={'home'}
                      title={'Enter Zipcode'}
                      name={'enter_zipcode'}
                  />
              </div>
              <Button title={'Find Bootcamps'} width={'100%'} className={'home'}></Button>
          </form>
        </div>
    )
}

export default Home