const validator = (password, confirmPassword) => {
    const pass = [...password]
    const conf = [...confirmPassword]
    for(let i = 0; i < conf.length; i++) {
        if(conf[i] !== pass[i]) {
            return false
        }
    }
        return true
}

export default validator